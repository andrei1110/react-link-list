export const Card = ({ children }: { children: React.ReactNode }) => (
  <div className="w-full max-w-md mx-auto bg-zinc-950 p-8 rounded-2xl shadow-lg border border-zinc-800">
    {children}
  </div>
);
